[package]
name = "stellar-vault-contract"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
soroban-sdk = { version = "23", features = ["testutils"] }

[dev-dependencies]
soroban-sdk = { version = "23", features = ["testutils"] }